//! `clippod` — the clipboard daemon. Owns the store, spawns the Wayland thread and
//! serves `com.nilfactor.Clippo` on the session bus.
//!
//! Placeholder — filled in at M3 (daemon + CLI); see `docs/ROADMAP.md`.

fn main() {
    println!("clippod: not implemented yet (arrives in M3 — see docs/ROADMAP.md)");
}
